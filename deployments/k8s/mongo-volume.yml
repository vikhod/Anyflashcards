apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-volume
spec:
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  local:
    path: /home/ubuntu/anyflashcards_volume